function testToStringCase(){
    let a = 1
    let text = ''
    switch(a){
        case a < 3: text = 'one'; break;
        // case 2: text = 'two'; break;
        // case 3: text = 'three'; break;
        // default: text = 'nishto'
        // case 5 < a < 10: text = ' meju 5 i 10'; break;
        // case 10 < a < 15: text = 'mejdu 10 i 15'; break;
    
    }
    return text;
}

console.log(testToStringCase());